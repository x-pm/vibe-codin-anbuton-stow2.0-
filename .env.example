# --- 真机 Expo Go 调试（若一直「Opening project」超时）---
# 在 cmd 运行 ipconfig，把 WLAN 的 IPv4 填到下面（示例 10.141.15.246），保存后重启 Metro
# REACT_NATIVE_PACKAGER_HOSTNAME=10.141.15.246
# 使用 Clash 时：开发阶段请关闭 TUN/系统代理，或为 10.0.0.0/8 设 DIRECT

# 硅基流动 SiliconFlow（国内，注册后有免费额度）：https://siliconflow.cn
# 密钥只能在官网个人中心创建，不能共用他人 Key；把下面等号右侧换成你的 sk- 开头密钥。
EXPO_PUBLIC_SILICONFLOW_API_KEY=

# 可选：默认已指向 https://api.siliconflow.cn/v1
# EXPO_PUBLIC_SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# 纯文本解析（条码文本等）：默认 Qwen/Qwen2.5-7B-Instruct
# EXPO_PUBLIC_SILICONFLOW_MODEL=Qwen/Qwen2.5-7B-Instruct

# 拍照识别（多模态）：须选「模型广场」中带视觉标签的模型，例如：
# EXPO_PUBLIC_SILICONFLOW_VISION_MODEL=Qwen/Qwen3-VL-30B-A3B-Instruct
# 若该模型不可用：https://docs.siliconflow.cn/cn/userguide/capabilities/multimodal-vision

# 链接录入专用（不配则：文本默认 Qwen2.5-14B，配图精炼默认 Qwen2.5-VL-32B；仍会以通用 MODEL/VISION 为第二优先级）
# EXPO_PUBLIC_SILICONFLOW_LINK_TEXT_MODEL=Qwen/Qwen2.5-14B-Instruct
# EXPO_PUBLIC_SILICONFLOW_LINK_VISION_MODEL=Qwen/Qwen2.5-VL-32B-Instruct

# --- 腾讯云开发 CloudBase（AI 走云函数，推荐）---
# 环境 ID（控制台环境概览复制）
EXPO_PUBLIC_CLOUDBASE_ENV_ID=
# 可发布密钥 / Publishable Key（控制台「API 密钥」；匿名登录与调函数用）
# EXPO_PUBLIC_CLOUDBASE_PUBLISHABLE_KEY=
# AI 云函数名（默认 stowyun，与控制台一致）
# EXPO_PUBLIC_CLOUDBASE_FUNCTION_NAME=stowyun
# 业务数据同步云函数名（默认 stowSync；需部署 cloudfunctions/stowSync）
# EXPO_PUBLIC_CLOUDBASE_SYNC_FUNCTION=stowSync
